#include <stdio.h>

/**
 * main - Entry point of the program.
 * @argc: Arguements counter.
 * @argv: A pointer to the array of arguements entered.
 *
 * Return: 0 upon success else 1.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
