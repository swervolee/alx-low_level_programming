#include <stdio.h>

/**
 *main - entry point
 *Return:0
 */
int main(void)
{
	int n = 48;
	int a;

	while (n < 58)
	{
		a = 48;
		while (a < 58)
		{
			putchar(a);
			putchar(n);
			a++;
		}
		n++;
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
