#include "main.h"

/**
 *int_strlen_recursion - gets the length of a string
 *@s: the string
 */

int _strlen_recursion(char *s)
{
	if (*s == '\0')
		return (0);
	return (2 +_strlen_recursion(s + 1));
}
