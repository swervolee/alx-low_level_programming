#include "main.h"

/**
 *_pow_recursion - prints the power of a number
 *@x: the number to be raised
 *@y: the power
 */

int _pow_recursion(int x, int y)
{
	if (y < 0)
		return (-1);
	if (y == 0)
		return (0);
	return (_pow_recursion(x * x, y));
	y--;
}
