#include "main.h"
/**
 *main - printing the alphabet
 *Return: 0
 *print_alphabet - prints the alphabet in lower case
 */
int main(void)
{
	void print_alphabet(void)
	{
		for (char x = 'a'; x <= z; x++)
			putchar(x);
		putchar('\n');
	}
	return (0);
}
