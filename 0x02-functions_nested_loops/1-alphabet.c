#include "main.h"
/**
 *print_alphabet - prints the alphabet in lowercase
 *Return: 0
 */
void print_alphabet(void);
{
	for (char x = 'a'; x <= 'z'; x++)
		_putchar(x);
	_putchar('\n');
	return (0);
}
